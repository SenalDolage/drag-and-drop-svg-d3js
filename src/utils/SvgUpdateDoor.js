import * as d3 from "d3";

/**
 * Update a node.
 * Each time this is called we can update a particular element's location from Svg
 */
export default class SvgUpdateDoor {
  static update(doorId) {

  }
}
